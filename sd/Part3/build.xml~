<?xml version = "1.0"?>
<project name = "JumperRunner" default = "jumper" basedir = ".">
  <description>run the bugs </description>
  <property name = "jumper.dir" location = "jumper"/>
  <property name = "boxBug.dir" location = "boxBug"/>
  <property name = "lib.dir" location = "lib"/>
  <property name = "class.dir" location = "class"/>
  <path id = "compile.path">
    <fileset dir = "${lib.dir}">
      <include name = "**/*.jar"/>
    </fileset>
  </path>
  
  <target name = "clean">
    <delete dir = "${class.dir}"/>
    <mkdir dir = "${class.dir}"/>
  </target>

  <target name = "jumper" depends = "clean">
    <javac srcdir = "${jumper.dir}" destdir = "${class.dir}" classpathref = "compile.path" includeantruntime = "true">
      <compilerarg value="-Xlint:unchecked"/>
    </javac>
    <java classname = "JumperRunner" fork = "true">
      <classpath path = "${class.dir}"/>
      <classpath refid = "compile.path"/>
    </java>
  </target>

  <target name = "boxBug" depends = "clean">
    <javac srcdir = "${boxBug.dir}" destdir = "${class.dir}" classpathref = "compile.path" includeantruntime = "true">
      <compilerarg value="-Xlint:unchecked"/>
    </javac>
    <java classname = "BoxBugRunner" fork = "true">
      <classpath path = "${class.dir}"/>
      <classpath refid = "compile.path"/>
    </java>
  </target>
</project>